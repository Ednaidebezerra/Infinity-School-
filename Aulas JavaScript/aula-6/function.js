
//         nome é um parametro /variavel
/*function saudacao (nome){
    console.log (`Ola ${nome} !`)

}

const nomeUsuario = "Edna"
saudacao (nomeUsuario) */


/*function calcularDesconto ( preco, desconto){
   const desconto = preco * ( desconto/100)
   const valorFinal = preco - desconto
   console.log (` O valor com desnconto de  ${desconto}%: ${preco}`) 
}

const preco= parseFloat(prompt("Digite o preço do prduto"))
const preco= parseFloat(prompt("Digite o desconto em %")) */

// função soma
// function calcularSoma ( ...numeros){
//     let soma = 0
//     for( const numero of numeros)
//         soma += numeros
//     console.log ( `A soma dos numeros é: ${soma} `)
// }
   
// calcularSoma ( 50, 20, 30, 200,)

const filtroPares = (arrayNumeros)
for ( const numero of numeros) {
    const arrayPares = []
    if (numeros[i] % 2 ===0){
       arrayPares.push(numero)
    }
    return arrayPares
}
console.log(filtroPares([ 1,2,3,5,6,9,8]))


